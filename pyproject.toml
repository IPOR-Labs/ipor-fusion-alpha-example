[project]
name = "ipor-fusion-alpha-example"
version = "0.1.0"
description = "Example bot for IPOR Fusion Alpha using the IPOR Fusion SDK"
readme = "README.md"
license = "BSD-3-Clause"
authors = [{ name = "adam ipor", email = "adam@ipor.io" }]
requires-python = ">=3.11"
dependencies = [
    "ipor-fusion==3.6.4",
    "python-dotenv==1.1.0",
    "schedule==1.2.2",
]

[dependency-groups]
dev = [
    "pytest==8.3.5",
]
